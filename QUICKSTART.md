# Quick Start

```bash
docker compose up -d
```

Wait ~10 minutes on first run (image builds), then open:

**http://localhost:3010**

Login: `admin@nexus.local` / `admin123`

Verify:
```powershell
pwsh ./test_integration.ps1
```
